<Page x:Class="CanvasDrag.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:controls="using:CanvasDrag.Controls"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
      mc:Ignorable="d">

    <Grid x:DefaultBindMode="OneWay">
        <controls:DragCanvas AssociatedElement="{x:Bind draggableButton}">
            <Button x:Name="draggableButton"
                    Content="Drag me honey"
                    CornerRadius="4" />
        </controls:DragCanvas>
    </Grid>
</Page>